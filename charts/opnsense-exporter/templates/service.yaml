apiVersion: v1
kind: Service
metadata:
  name: opnsense-exporter
  namespace: monitoring
  labels:
    app: opnsense-exporter
spec:
  ports:
    - port: 8080
      targetPort: metrics
      name: metrics
  selector:
    app: opnsense-exporter
