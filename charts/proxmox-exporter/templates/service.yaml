apiVersion: v1
kind: Service
metadata:
  name: proxmox-exporter
  namespace: monitoring
  labels:
    app: proxmox-exporter
spec:
  ports:
    - port: 9221
      targetPort: metrics
      name: metrics
  selector:
    app: proxmox-exporter
